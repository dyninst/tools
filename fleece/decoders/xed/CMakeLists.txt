    
IF(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT)
    SET(XED_DIR "" CACHE STRING "Base directory for xed" FORCE)
ENDIF()

IF(NOT XED_DIR STREQUAL "")

    MESSAGE(STATUS "Using decoder: xed")

    # Set the sources that should be compiled into the library
    SET(FLEECE_DECODER_SOURCE ${FLEECE_DECODER_SOURCE} xed/xed_x86.C PARENT_SCOPE)

    SET(FLEECE_DECODER_LIBRARIES ${FLEECE_DECODER_LIBRARIES} xed PARENT_SCOPE)

    SET(DECODER_INSTALL_DIRS ${DECODER_INSTALL_DIRS} ${XED_DIR} PARENT_SCOPE)

ENDIF()
